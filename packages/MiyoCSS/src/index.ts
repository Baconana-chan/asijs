/**
 * miyocss — SSR-first CSS + SVG framework.
 *
 * Main entry exports the framework-agnostic core only.
 * Adapters and modules are subpath imports:
 *   - `miyocss/asi` — AsiJS adapter
 *   - `miyocss/svg` — SVG module
 */

export * from "./core";
