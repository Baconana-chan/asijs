/**
 * miyocss/svg — SVG module (first-class, not an afterthought).
 *
 * Pure SVG output, no client JS, SSR-safe.
 *
 * Planned (see TODO.md P1.3):
 *  - `svg()` helpers: primitives, gradients, filters
 *  - CSS-properties vs presentation-attributes contract
 *  - Icon set (currentColor, token-sized)
 *  - Chart primitives: bar / line / pie
 *  - Animated SVG (`<animate>`, `<animateTransform>`)
 */
